import { messages as cMessages } from '../../c-lib/locale/en-US/index'

const messages = {
    c: cMessages,
}

const numberFormats = {}

const dateTimeFormats = {}

const enUS = {
    messages,
    numberFormats,
    dateTimeFormats,
}

export default enUS